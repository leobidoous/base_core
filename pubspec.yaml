name: base_core
description: Um package auxiliar para manter as dependências globais de um aplicativo multi-repositório e mono-repositório.
homepage: https://github.com/leobidoous
repository: https://github.com/leobidoous/core
issue_tracker: https://github.com/leobidoous/core/issues
version: 1.0.12

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  #===== Firebase depedencies ===================
  firebase_auth: ^4.19.2
  firebase_core: ^2.30.0
  cloud_firestore: ^4.17.0
  firebase_storage: ^11.7.2
  firebase_database: ^10.5.2
  firebase_messaging: ^14.8.2
  firebase_analytics: ^10.10.2
  firebase_crashlytics: ^3.5.2
  firebase_in_app_messaging: ^0.7.5+2
  #==============================================

  #===== New dependecies for new app flutter =====
  dio: ^5.4.3+1
  intl: ^0.18.1
  rxdart: ^0.27.7
  equatable: ^2.0.5
  share_plus: ^8.0.3
  local_auth: ^2.2.0
  geolocator: ^11.0.0
  path_provider: ^2.1.3
  sentry_flutter: ^7.20.0
  local_auth_darwin: ^1.2.2
  connectivity_plus: ^6.0.2
  package_info_plus: ^7.0.0
  shared_preferences: ^2.2.3
  permission_handler: ^11.3.1
  local_auth_android: ^1.0.38
  flutter_secure_storage: ^9.0.0
  mask_text_input_formatter: ^2.9.0
  flutter_local_notifications: ^17.0.1
  currency_text_input_formatter: ^2.2.1
  #==============================================

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
